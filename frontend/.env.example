# Local development uses relative /v1 requests through the Vite proxy.
# Leave this empty unless you intentionally need direct absolute API URLs.
VITE_API_BASE_URL=''

# Tencent Map key (optional)
VITE_QQ_MAP_KEY='YOUR_QQ_MAP_KEY'
